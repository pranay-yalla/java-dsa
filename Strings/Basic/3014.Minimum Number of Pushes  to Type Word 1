class Solution {
    public int minimumPushes(String s) {
        if(s.length()<=8)
return s.length();
else if(s.length()<=16)
return  (s.length()-8)*2+8;
else if(s.length()<=24)
return  (s.length()-16)*3+24;
else
return  (s.length()-24)*4+48;
    }
}
